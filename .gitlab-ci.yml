image: node:lts-alpine

stages:
  - install_deps
  - build

install_dependencies:
  stage: install_deps
  script:
    - npm install
  artifacts:
    paths:
      - node_modules

build:
  stage: build
  script: 
    - npm run build

